/** The VS Code settings for this workspace **/
{

    /* Shell environment activation */

    // Set that shell integration is enabled,
    // as the custom scripts always make use of its scripts
    "terminal.integrated.shellIntegration.enabled": true,

    // Disable Python venv auto-activation,
    // as it will be activated in the custom initialization files
    "python.terminal.activateEnvironment": false,
    "python.terminal.activateEnvInCurrentTerminal": false,

    // Modify shell profiles to use custom initialization files
    "terminal.integrated.profiles.linux": {
        "bash": {
            "args": ["--init-file", "${workspaceFolder}/.vscode/.bashrc"],
        },
        "zsh": {
            "env": {
                "ZDOTDIR": "${workspaceFolder}/.vscode",
                "ANSIBLESITE_USER_ZDOTDIR": "${env:ZDOTDIR}"
            }
        }
    },
    "terminal.integrated.profiles.osx": {
        "bash": {
            "args": ["--init-file", "${workspaceFolder}/.vscode/.bashrc"]
        },
        "zsh": {
            "env": {
                "ZDOTDIR": "${workspaceFolder}/.vscode",
                "ANSIBLESITE_USER_ZDOTDIR": "${env:ZDOTDIR}"
            }
        }
    },

    
    /* Ansible extension */

    // Configure the Ansible extension to use the activation script
    "ansible.python.activationScript": "bin/activate",


    /* Language mode */

    // Set the language mode for Ansible-related files
    "files.associations": {
        "**/playbooks/**/*.{yml,yaml}": "ansible",
        "**/inventory/**/*.{yml,yaml}": "ansible",
        "**/inventory/**/*": "ini",
        "**/roles/*/{tasks,handlers,vars,defaults,meta}/**/*.{yml,yaml}": "ansible",
        "**/roles/*/templates/**/*.j2": "ansible-jinja"
    }

}
